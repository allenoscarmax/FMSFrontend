<Window x:Class="FMSFrontend.Views.Windows.ReviseProcessWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" mc:Ignorable="d" Title="流程修正" Width="1200" Height="450" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent" MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="8" ShadowDepth="2" Color="Black" Opacity="0.25" />

        <SolidColorBrush x:Key="CardUndoBrush" Color="#BFE7F2" />
        <SolidColorBrush x:Key="CardNextBrush" Color="#F6B9F2" />
        <SolidColorBrush x:Key="CardForceBrush" Color="#E2E2E2" />
    </Window.Resources>

    <Border Background="White" CornerRadius="10" Effect="{StaticResource ShadowEffect}">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="56" />
                <RowDefinition Height="*" />
                <RowDefinition Height="80" />
            </Grid.RowDefinitions>

            <!-- ===== Title Bar ===== -->
            <Border Grid.Row="0" Background="#444" CornerRadius="10,10,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="48" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                        <materialDesign:PackIcon Kind="ClipboardEditOutline" Width="24" Height="24" Foreground="White" />
                        <TextBlock Text="流程修正" Foreground="White" FontSize="20" FontWeight="Bold" Margin="10,0,0,0" />
                    </StackPanel>

                    <Button Content="✕" Grid.Column="1" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="36" FontWeight="Bold" Cursor="Hand" Click="Close_Click" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>
            </Border>

            <!-- ===== Main Content ===== -->
            <Grid Grid.Row="1" Margin="24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Undo -->
                <Border Grid.Column="0" Background="{StaticResource CardUndoBrush}" CornerRadius="10" Padding="24">
                    <Button Style="{StaticResource PrimaryButtonStyle}" Background="Transparent" BorderThickness="0" Command="{Binding UndoElectrodeCommand}">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="撤回本支電極" FontSize="22" FontWeight="Bold" TextDecorations="Underline" TextAlignment="Center" />
                            <TextBlock Text="請將目前在 EDM 機台上的電極卸下，並把與本支同名的電極放回倉。" Margin="0,14,0,0" FontSize="15" TextWrapping="Wrap" TextAlignment="Center" />
                        </StackPanel>
                    </Button>
                </Border>

                <!-- Select Next -->
                <Border Grid.Column="2" Background="{StaticResource CardNextBrush}" CornerRadius="10" Padding="24">
                    <Button Style="{StaticResource PrimaryButtonStyle}" Background="Transparent" BorderThickness="0" Command="{Binding SelectNextElectrodeCommand}">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="選擇下一支電極" FontSize="22" FontWeight="Bold" TextDecorations="Underline" TextAlignment="Center" />
                            <TextBlock Text="請卸下目前電極並保持該電極不要進倉，系統將安裝下一步所需的正確電極。" Margin="0,14,0,0" FontSize="15" TextWrapping="Wrap" TextAlignment="Center" />
                        </StackPanel>
                    </Button>
                </Border>

                <!-- Force Complete -->
                <Border Grid.Column="4" Background="{StaticResource CardForceBrush}" CornerRadius="10" Padding="24">
                    <Button Style="{StaticResource PrimaryButtonStyle}" Background="Transparent" BorderThickness="0" Command="{Binding ForceCompleteCommand}">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="強制完成流程" FontSize="22" FontWeight="Bold" TextDecorations="Underline" TextAlignment="Center" />
                            <TextBlock Text="請卸下目前電極並保持該電極不要進倉，流程將直接結束完成。" Margin="0,14,0,0" FontSize="15" TextWrapping="Wrap" TextAlignment="Center" />
                        </StackPanel>
                    </Button>
                </Border>
            </Grid>

            <!-- ===== Bottom ===== -->
            <StackPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button Content="取消" Width="140" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding CancelCommand}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
