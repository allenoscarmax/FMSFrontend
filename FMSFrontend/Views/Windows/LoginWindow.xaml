<Window x:Class="FMSFrontend.Views.Windows.LoginWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" mc:Ignorable="d" Title="登入" Height="320" Width="420" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Window.Resources>
        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="10" ShadowDepth="2" Color="Black" Opacity="0.2" />
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <Style x:Key="LoginButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#2779A7" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="20,10" />
            <Setter Property="MinWidth" Value="120" />
            <Setter Property="Height" Value="Auto" />
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#D8C7FF" />
                    <Setter Property="Foreground" Value="#666" />
                    <Setter Property="Cursor" Value="Arrow" />
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#368AB8" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LabelTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="Foreground" Value="#333" />
        </Style>

        <Style x:Key="InputControlStyle" TargetType="Control">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="Height" Value="36" />
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="8" Effect="{StaticResource ShadowEffect}" Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#444" CornerRadius="8,8,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="48" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                        <TextBlock Text="系統登入" Foreground="White" FontSize="18" FontWeight="SemiBold" />
                    </StackPanel>
                    <Button Content="✕" Grid.Column="1" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="16" Cursor="Hand" Click="Close_Click" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                </Grid>
            </Border>

            <Border Grid.Row="1" Background="Transparent" Padding="30,20" Margin="0,0,0,5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="20" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="15" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="使用者：" Style="{StaticResource LabelTextStyle}" />
                    <TextBox Grid.Row="0" Grid.Column="2" Text="{Binding Name}" Style="{StaticResource InputControlStyle}" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="密碼：" Style="{StaticResource LabelTextStyle}" />
                    <PasswordBox Grid.Row="2" Grid.Column="2" x:Name="PasswordBox" PasswordChar="●" Style="{StaticResource InputControlStyle}" PasswordChanged="PasswordBox_PasswordChanged" />

                    <TextBlock Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Foreground="#D32F2F" Text="{Binding Message}" VerticalAlignment="Top" HorizontalAlignment="Center" FontSize="14" TextWrapping="Wrap" Margin="0,10,0,0" />

                    <Button Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" Content="登入" Style="{StaticResource LoginButtonStyle}" Command="{Binding LoginClickCommand}" IsDefault="True" VerticalAlignment="Bottom" Margin="0,0,0,55" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>