<UserControl x:Class="FMSFrontend.Views.OperationHistory" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" mc:Ignorable="d" d:DesignHeight="912" d:DesignWidth="1532">

    <UserControl.Resources>
        <!-- 白底卡片外觀 -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#E5E7EB" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
        </Style>

        <!-- DataGrid 樣式 -->
        <Style x:Key="HistoryDataGridStyle" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="CanUserReorderColumns" Value="False" />
            <Setter Property="CanUserResizeRows" Value="False" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="#E5E7EB" />
            <Setter Property="RowHeight" Value="44" />
            <Setter Property="ColumnHeaderHeight" Value="36" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="White" />
            <Setter Property="RowBackground" Value="White" />
            <Setter Property="AlternatingRowBackground" Value="#FAFAFA" />
        </Style>

        <Style x:Key="HistoryHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#53565A" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="HistoryCellStyle" TargetType="DataGridCell">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="12,0,12,0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="#1F2937" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
        </Style>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <Border Background="{DynamicResource PageViewBrush}">
        <Grid Background="{DynamicResource PageViewBrush}" Margin="16,20,16,18">
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <!-- 標題列 -->
                <RowDefinition Height="40" />
                <!-- 工具列 -->
                <RowDefinition Height="*" />
                <!-- 主內容 -->
            </Grid.RowDefinitions>

            <!-- 標題列 -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0">
                <materialDesign:PackIcon Kind="History" Width="24" Height="24" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,4,4,0" />
                <TextBlock Text="{DynamicResource OperationHistory_Title}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" />
            </StackPanel>

            <!-- 工具列 -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="130" />
                    <ColumnDefinition Width="130" />
                </Grid.ColumnDefinitions>

                <!-- 選擇日期 -->
                <StackPanel  Grid.Column="0" Orientation="Horizontal">
                    <ComboBox Width="140" Height="32" Margin="10,0,0,0" SelectedIndex="{Binding SelectedFilterIndex}" Style="{StaticResource PillComboBoxStyle}">
                        <ComboBoxItem Content="{DynamicResource OperationHistory_DateFilter_Today}" />
                        <ComboBoxItem Content="{DynamicResource OperationHistory_DateFilter_Last7Days}" />
                        <ComboBoxItem Content="{DynamicResource OperationHistory_DateFilter_Custom}" />
                    </ComboBox>
                    <StackPanel Orientation="Horizontal" Visibility="{Binding IsCustomDateMode, Converter={StaticResource BoolToVisibilityConverter}}">
                        <DatePicker Width="180" Height="32" Margin="8,0,0,0" SelectedDate="{Binding FromDate}"  IsEnabled="{Binding IsCustomDateMode}" Style="{StaticResource CustomDatePickerStyle}" />
                        <materialDesign:PackIcon Kind="ArrowRight" Width="18" Height="18" VerticalAlignment="Center" Margin="6,0,6,0" />
                        <DatePicker Width="180" Height="32" SelectedDate="{Binding ToDate}" IsEnabled="{Binding IsCustomDateMode}" Style="{StaticResource CustomDatePickerStyle}" />
                    </StackPanel>
                </StackPanel>
                
                <!-- 匯出 -->
                <Button Grid.Column="1" Margin="5,0" Command="{Binding ExportCommand}" Background="#1976D2" Foreground="White" Cursor="Hand" Style="{StaticResource PrimaryButtonStyle}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="Export" Width="18" Height="18" Margin="0,0,6,0" />
                        <TextBlock Text="{DynamicResource OperationHistory_Button_Export}" />
                    </StackPanel>
                </Button>

                <!-- 清除 -->
                <Button Grid.Column="2"  Margin="5,0" Command="{Binding ClearCommand}" Background="#444444" Foreground="White" Padding="10,0" Cursor="Hand" Style="{StaticResource PrimaryButtonStyle}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="Delete" Width="18" Height="18" Margin="0,0,6,0" />
                        <TextBlock Text="{DynamicResource OperationHistory_Button_Clear}" />
                    </StackPanel>
                </Button>
            </Grid>

            <!-- 📑 表格（改用與 RFID 相同風格） -->
            <Border Grid.Row="2" CornerRadius="0" BorderBrush="#CCC" BorderThickness="1" Background="White" SnapsToDevicePixels="True" Margin="0,20,0,0">

                <DataGrid ItemsSource="{Binding OperationRecords}" AutoGenerateColumns="False" IsReadOnly="True" CanUserResizeColumns="False" CanUserReorderColumns="False" CanUserSortColumns="False" HeadersVisibility="Column" SelectionMode="Single" SelectionUnit="FullRow" RowHeight="36" ColumnHeaderHeight="40" FontSize="16" Style="{StaticResource ModernFlatDataGridStyle}" ColumnHeaderStyle="{StaticResource CompactDataGridColumnHeaderStyle}" GridLinesVisibility="None" HorizontalGridLinesBrush="Transparent" VerticalGridLinesBrush="Transparent" SnapsToDevicePixels="True">

                    <!-- Cell 邊框/內距 -->
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="Padding" Value="6,0,6,0" />
                            <Setter Property="SnapsToDevicePixels" Value="True" />
                        </Style>
                    </DataGrid.CellStyle>

                    <!-- Row 邊框/間距 -->
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="SnapsToDevicePixels" Value="True" />
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Columns>
                        <!-- 時間：置中 -->
                        <DataGridTextColumn Header="{DynamicResource OperationHistory_Column_Time}" Binding="{Binding TimeText}" Width="260" ElementStyle="{StaticResource CenterAlignedTextStyle}" CellStyle="{StaticResource CenterAlignedCellStyle}" />

                        <!-- 使用者：置中 -->
                        <DataGridTextColumn Header="{DynamicResource OperationHistory_Column_User}" Binding="{Binding User}" Width="180" ElementStyle="{StaticResource CenterAlignedTextStyle}" CellStyle="{StaticResource CenterAlignedCellStyle}" />

                        <!-- 訊息：置左、可撐滿 -->
                        <DataGridTextColumn Header="{DynamicResource OperationHistory_Column_Message}" Binding="{Binding Message}" Width="*" ElementStyle="{StaticResource CenterAlignedTextStyle}" CellStyle="{StaticResource CenterAlignedCellStyle}" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

        </Grid>
    </Border>
</UserControl>
