<UserControl x:Class="FMSFrontend.Views.ProductionLines" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" d:DesignHeight="912" d:DesignWidth="1532">

    <Border Background="{DynamicResource PageViewBrush}">
        <Grid Background="{DynamicResource PageViewBrush}" Margin="16,20">
            <!-- 40+724+90 = 854 -->
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <!-- 上方：Title -->
                <RowDefinition Height="724" />
                <!-- 中間：主內容 -->
                <RowDefinition Height="90" />
                <!-- 下方：手臂狀態 -->
            </Grid.RowDefinitions>

            <!-- 上方：Icon + 標題 -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0">
                <!-- MaterialDesign PackIcon -->
                <materialDesign:PackIcon Kind="Factory" Width="24" Height="24" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,8,0" />
                <TextBlock Text="產線總覽" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" />
            </StackPanel>
            
            <!-- ✅ 中間：內容容器 -->
            <Grid Grid.Row="1" Margin="0 10 0 0">
                <!-- 等一下這邊我們可以切左右欄位，或再塞 ItemsControl + WrapPanel -->
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="899" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ContentControl Grid.Column="0" Grid.Row="1" Background="White" Content="{Binding CurrentStorageView}" />

                <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="10" Margin="10,0,0,0">
                    <TextBlock Text="Working Zone Overview" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <!-- 可放機台卡片 -->
                </Border>
            </Grid>

            <!-- ✅ 下方：機械手臂狀態 -->
            <Grid Grid.Row="2" Background="Transparent"> 
                <Border Background="#CCCCCC" CornerRadius="10" Width="387" Height="100" Margin="0 10 0 -20" HorizontalAlignment="Left">
                    <StackPanel Orientation=" Horizontal" VerticalAlignment=" Center">
                        <Ellipse Width=" 14" Height=" 14" Fill=" Green" Margin="8,0" />
                        <Image Source="pack://application:,,,/FMSFrontend;component/Image/工具機-icon/01-手臂.png" Width="76" Height="84" />
                        <TextBlock Text="example" FontSize="16" Margin="12,0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>
