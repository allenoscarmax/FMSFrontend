<UserControl x:Class="FMSFrontend.Views.WorkOrder" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:local="clr-namespace:FMSFrontend.Converters" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:selectors="clr-namespace:FMSFrontend.Selectors" mc:Ignorable="d" d:DesignHeight="912" d:DesignWidth="1532">

    <UserControl.Resources>
        <selectors:TabItemBackgroundSelector x:Key="TabItemColorSelector" />
        <local:BoolToIconConverter x:Key="BoolToIconConverter" />
        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="8" ShadowDepth="2" Color="Black" Opacity="0.25" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Border Background="{DynamicResource PageViewBrush}">
        <Grid Margin="16,20,16,18">
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <!-- 標題列 -->
                <RowDefinition Height="50" />
                <!-- Tabs -->
                <RowDefinition Height="*" />
                <!-- 內容區（根據所選 Tab 切換） -->
            </Grid.RowDefinitions>

            <!-- 標題列 -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <materialDesign:PackIcon Kind="ClipboardListOutline" VerticalAlignment="Center" Width="36" Height="36" Margin="0,0,8,0" Foreground="{DynamicResource PrimaryTextBrush}" />
                <TextBlock Text="工單管理" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" />
            </StackPanel>

            <!-- Tabs -->
            <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTabIndexParameter}" Style="{StaticResource FlatTabControlStyle}" Height="40" Margin="0,8,0,0" Background="Transparent" ItemContainerStyleSelector="{StaticResource TabItemColorSelector}">
                <TabItem Header="New/新建" Tag="#2779A7" />
                <TabItem Header="EDM" Tag="#2779A7" />
                <TabItem Header="Failure/失敗" Tag="#2779A7" />
            </TabControl>

            <!-- 外部已經統一套好卡片框 -->
            <Border Background="White" CornerRadius="0,0,10,10" Padding="40,16,40,24" Effect="{StaticResource ShadowEffect}" Grid.Row="2">

                <!-- 切換內容區 -->
                <ContentControl Grid.Row="2" Content="{Binding}">
                    <ContentControl.Style>
                        <Style TargetType="ContentControl">
                            <Style.Triggers>
                                <!-- ✅ Tab 0：New -->                              
                                <DataTrigger Binding="{Binding SelectedTabIndexParameter}" Value="0">
                                    <Setter Property="ContentTemplate">
                                        <Setter.Value>
                                            <DataTemplate>
                                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="*" />
                                                        </Grid.RowDefinitions>

                                                        <!-- Upload 按鈕 -->
                                                        <Button Grid.Row="0" HorizontalAlignment="Left" Margin="0,0,0,16" Padding="16,10" Background="#0070E0" Foreground="White" FontWeight="Bold" Cursor="Hand" BorderThickness="0" Effect="{StaticResource ShadowEffect}">
                                                            <Border CornerRadius="8" Background="Transparent">
                                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                                    <materialDesign:PackIcon Kind="Upload" Width="20" Height="20" Margin="0,0,6,0" />
                                                                    <TextBlock Text="Upload/上傳" FontSize="14" VerticalAlignment="Center" />
                                                                </StackPanel>
                                                            </Border>
                                                        </Button>

                                                        <!-- 外框 -->
                                                        <Border Grid.Row="1" CornerRadius="10" BorderBrush="#CCC" BorderThickness="1" Background="White" SnapsToDevicePixels="True" Margin="0,0,0,8">
                                                            <Grid>
                                                                <DataGrid Name="NewWorkDataGrid" 
                                                                          ItemsSource="{Binding WorkOrderList}" IsReadOnly="True" AutoGenerateColumns="False" 
                                                                          RowDetailsVisibilityMode="Collapsed" CanUserResizeColumns="False" CanUserReorderColumns="False" CanUserSortColumns="False" SelectionMode="Single" SelectionUnit="FullRow" RowHeight="44">

                                                                    <DataGrid.Style>
                                                                        <Style TargetType="DataGrid" BasedOn="{StaticResource ModernFlatDataGridStyle}" />
                                                                    </DataGrid.Style>
                                                                    <!-- ✅ RowStyle 控制展開 -->
                                                                    <DataGrid.RowStyle>
                                                                        <Style TargetType="DataGridRow">
                                                                            <Setter Property="DetailsVisibility">
                                                                                <Setter.Value>
                                                                                    <Binding Path="IsExpanded" Converter="{StaticResource BooleanToVisibilityConverter}" />
                                                                                </Setter.Value>
                                                                            </Setter>
                                                                        </Style>
                                                                    </DataGrid.RowStyle>
                                                                    <!-- ✅ Columns 開始 -->
                                                                    <DataGrid.Columns>
                                                                        <DataGridTemplateColumn Width="40">
                                                                            <DataGridTemplateColumn.CellTemplate>
                                                                                <DataTemplate>
                                                                                    <Border Tag="ExpandClickArea">
                                                                                        <!-- ← Tag 是關鍵 -->
                                                                                        <materialDesign:PackIcon Width="20" Height="20">
                                                                                            <materialDesign:PackIcon.Style>
                                                                                                <Style TargetType="materialDesign:PackIcon">
                                                                                                    <Setter Property="Kind" Value="ChevronRight" />
                                                                                                    <Style.Triggers>
                                                                                                        <DataTrigger Binding="{Binding IsExpanded}" Value="True">
                                                                                                            <Setter Property="Kind" Value="ChevronDown" />
                                                                                                        </DataTrigger>
                                                                                                    </Style.Triggers>
                                                                                                </Style>
                                                                                            </materialDesign:PackIcon.Style>
                                                                                        </materialDesign:PackIcon>
                                                                                    </Border>
                                                                                </DataTemplate>
                                                                            </DataGridTemplateColumn.CellTemplate>
                                                                        </DataGridTemplateColumn>

                                                                        <DataGridTextColumn Header="工單編號" Binding="{Binding OrderNumber}" Width="150" ElementStyle="{StaticResource CenterAlignedTextStyle}" CellStyle="{StaticResource CenterAlignedCellStyle}" />

                                                                        <DataGridTextColumn Header="零件名稱" Binding="{Binding PartName}" Width="150" ElementStyle="{StaticResource CenterAlignedTextStyle}" CellStyle="{StaticResource CenterAlignedCellStyle}" />

                                                                        <DataGridTemplateColumn Header="狀態" Width="180">
                                                                            <DataGridTemplateColumn.CellTemplate>
                                                                                <DataTemplate>
                                                                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                                                        <Ellipse Width="10" Height="10" Fill="{Binding StatusColor}" Margin="0,0,6,0" VerticalAlignment="Center" />
                                                                                        <TextBlock Text="{Binding Status}" FontSize="16" VerticalAlignment="Center" />
                                                                                    </StackPanel>
                                                                                </DataTemplate>
                                                                            </DataGridTemplateColumn.CellTemplate>
                                                                        </DataGridTemplateColumn>

                                                                        <DataGridTextColumn Header="目標EDM" Binding="{Binding TargetEDM}" Width="100" ElementStyle="{StaticResource CenterAlignedTextStyle}" CellStyle="{StaticResource CenterAlignedCellStyle}" />

                                                                        <DataGridTextColumn Header="座標" Binding="{Binding Coordinate}" Width="80" ElementStyle="{StaticResource CenterAlignedTextStyle}" CellStyle="{StaticResource CenterAlignedCellStyle}" />

                                                                        <DataGridTextColumn Header="設定者" Binding="{Binding Operator}" Width="100" ElementStyle="{StaticResource CenterAlignedTextStyle}" CellStyle="{StaticResource CenterAlignedCellStyle}" />

                                                                        <DataGridTemplateColumn Header="刪除" Width="60">
                                                                            <DataGridTemplateColumn.CellTemplate>
                                                                                <DataTemplate>
                                                                                    <Button Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" ToolTip="刪除">
                                                                                        <materialDesign:PackIcon Kind="TrashCanOutline" Foreground="#0070E0" Width="20" Height="20" />
                                                                                    </Button>
                                                                                </DataTemplate>
                                                                            </DataGridTemplateColumn.CellTemplate>
                                                                        </DataGridTemplateColumn>
                                                                    </DataGrid.Columns>

                                                                    <DataGrid.RowDetailsTemplate >
                                                                        <DataTemplate >
                                                                            <StackPanel Margin="20,2,20,2" Background="#F9F9F9">
                                                                                <StackPanel Orientation="Horizontal" Margin="0,20,0,20" VerticalAlignment="Center" >
                                                                                    <TextBlock Text="{Binding EDMStageDisplay}" FontSize="14" FontWeight="Bold" Foreground="#333" VerticalAlignment="Center" Margin="0,0,8,0" />
                                                                                    <ProgressBar Value="{Binding EDMStageProgress, Mode=OneWay}" Height="8" Width="200" VerticalAlignment="Center" />
                                                                                </StackPanel>

                                                                                <DataGrid IsReadOnly="True" ItemsSource="{Binding DataContext.EDMDetails, RelativeSource={RelativeSource AncestorType=DataGridRow}}" Style="{StaticResource ModernFlatDataGridStyle}" AutoGenerateColumns="False" CanUserResizeColumns="False" CanUserReorderColumns="False" CanUserSortColumns="False" HeadersVisibility="Column" FontSize="14" Margin="0">
                                                                                    <DataGrid.RowStyle>
                                                                                        <Style TargetType="DataGridRow">
                                                                                            <Setter Property="MinHeight" Value="40" />
                                                                                            <Setter Property="Padding" Value="2" />
                                                                                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                                                                                            <Setter Property="VerticalAlignment" Value="Center" />
                                                                                            <Setter Property="BorderThickness" Value="1" />
                                                                                            <Setter Property="BorderBrush" Value="Black" />
                                                                                        </Style>
                                                                                    </DataGrid.RowStyle>

                                                                                    <DataGrid.Columns>
                                                                                        <DataGridTextColumn Header="電極名稱" Binding="{Binding ElectrodeName}" Width="120" ElementStyle="{StaticResource CenterAlignedTextStyle}" CellStyle="{StaticResource CenterAlignedCellStyle}" />
                                                                                        <DataGridTextColumn Header="標籤序號" Binding="{Binding LabelSerial}" Width="120" ElementStyle="{StaticResource CenterAlignedTextStyle}" CellStyle="{StaticResource CenterAlignedCellStyle}"/>
                                                                                        <DataGridTextColumn Header="狀態" Binding="{Binding Status}" Width="100" ElementStyle="{StaticResource CenterAlignedTextStyle}" CellStyle="{StaticResource CenterAlignedCellStyle}" />
                                                                                        <DataGridCheckBoxColumn Header="需EDM" IsReadOnly="False" Binding="{Binding NeedEDM}" Width="120" ElementStyle="{StaticResource CenterAlignedCheckBoxStyle}" EditingElementStyle="{StaticResource CenterAlignedCheckBoxStyle}" CellStyle="{StaticResource CenterAlignedCheckBoxCellStyle}" />
                                                                                        <DataGridTextColumn Header="加工程式" Binding="{Binding EDMProgram}" Width="150" ElementStyle="{StaticResource CenterAlignedTextStyle}" CellStyle="{StaticResource CenterAlignedCellStyle}" />
                                                                                        <DataGridTextColumn Header="Offset" Binding="{Binding Offset}" Width="80" ElementStyle="{StaticResource CenterAlignedTextStyle}" CellStyle="{StaticResource CenterAlignedCellStyle}" />
                                                                                    </DataGrid.Columns>
                                                                                </DataGrid>
                                                                            </StackPanel>
                                                                        </DataTemplate>
                                                                    </DataGrid.RowDetailsTemplate>
                                                                </DataGrid>
                                                            </Grid>
                                                        </Border>
                                                    </Grid>
                                                </ScrollViewer>
                                            </DataTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>

                                <!-- ✅ Tab 1：EDM -->
                                <DataTrigger Binding="{Binding SelectedTabIndexParameter}" Value="1">
                                    <Setter Property="ContentTemplate">
                                        <Setter.Value>
                                            <DataTemplate>
                                                <Grid>
                                                    <!-- 放 EDM 清單、Queue、排序按鈕等等 -->
                                                    <TextBlock Text="EDM內容區，待實作..." Foreground="Gray" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                </Grid>
                                            </DataTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>

                                <!-- ✅ Tab 2：Failure -->
                                <DataTrigger Binding="{Binding SelectedTabIndexParameter}" Value="2">
                                    <Setter Property="ContentTemplate">
                                        <Setter.Value>
                                            <DataTemplate>
                                                <Grid>
                                                    <!-- 放 Action 按鈕 DataGrid 等 -->
                                                    <TextBlock Text="Failure內容區，待實作..." Foreground="Gray" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                </Grid>
                                            </DataTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>

                            </Style.Triggers>
                        </Style>
                    </ContentControl.Style>
                </ContentControl>
            </Border>
        </Grid>

    </Border>
</UserControl>
