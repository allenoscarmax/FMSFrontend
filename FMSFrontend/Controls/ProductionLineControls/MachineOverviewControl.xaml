<UserControl x:Class="FMSFrontend.Controls.MachineOverviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FMSFrontend.Controls" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" mc:Ignorable="d" d:DesignHeight="714" d:DesignWidth="601" Background="Transparent">
    <UserControl.Resources>
        

    </UserControl.Resources>

    <Grid>
        <!-- 陰影層 -->
        <Border Background="#33000000" CornerRadius="18" Margin="-2,-2,-2,-2" />
        <Border CornerRadius="18" Background="#FFFFFF" BorderBrush="#DDD" ClipToBounds="True" SnapsToDevicePixels="True" />

        <Grid ClipToBounds="True" SnapsToDevicePixels="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="44" />
                <!-- 上方標題列 -->
                <RowDefinition Height="*" />
                <!-- 下方卡片區 -->
            </Grid.RowDefinitions>
            <!-- ✅ 標題列 -->
            <Border Grid.Row="0" CornerRadius="18,18,0,0" Background="#EEEEEE" SnapsToDevicePixels="True" ClipToBounds="True">
                <Grid Height="40" Margin="0,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Working Zone Overview" FontSize="16" FontWeight="Bold" Foreground="#333" VerticalAlignment="Center" Margin="20,0,0,0" />
                    <Button Grid.Column="1" Width="28" Height="28" Margin="0,0,20,0" HorizontalAlignment="Right" Background="Transparent" BorderBrush="{x:Null}" ToolTip="展開/收合倉儲" Command="{Binding ToggleExpandCommand}">
                        <materialDesign:PackIcon Kind="ViewGrid" Width="24" Height="24" Foreground="#555" />
                    </Button>
                </Grid>
            </Border>

            <!-- 🔹 Row 1：機台卡片列表 -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="16,0,16,8" HorizontalAlignment="Center">

                <ItemsControl ItemsSource="{Binding Machines}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:MachineCardControl Width="260" Height="120" Margin="12,8" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

    </Grid>
</UserControl>
