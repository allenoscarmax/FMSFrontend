<Window x:Class="FMSFrontend.Extensions.SelectSharedElectrodeWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" mc:Ignorable="d" Title="選擇共用電極" Width="760" Height="400" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent" MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="8" ShadowDepth="2" Color="Black" Opacity="0.25" />
    </Window.Resources>

    <Border Background="White" CornerRadius="10" Effect="{StaticResource ShadowEffect}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="56" />
                <RowDefinition Height="*" />
                <RowDefinition Height="80" />
            </Grid.RowDefinitions>

            <!-- ✅ 標題列 -->
            <Border Background="#444" CornerRadius="10,10,0,0" Grid.Row="0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="48" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                        <materialDesign:PackIcon Kind="Link" Width="24" Height="24" Foreground="White" />
                        <TextBlock Text="選擇共用電極" Foreground="White" FontSize="20" FontWeight="Bold" Margin="10,0,0,0" />
                    </StackPanel>

                    <Button Content="✕" Grid.Column="1" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="16" FontWeight="Bold" Cursor="Hand" Click="Close_Click" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>
            </Border>

            <!-- ✅ 主內容 -->
            <Grid Grid.Row="1" Margin="30,20,30,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="48" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="48" />
                </Grid.RowDefinitions>

                <!-- 標題：選擇工單 -->
                <TextBlock Grid.Row="0" Text="選擇工單" FontSize="16" FontWeight="Bold" Margin="0,0,0,8" Foreground="#444" />

                <!-- 下拉按鈕：工單 -->
                <Button Grid.Row="1" Content="{Binding SelectedWorkOrderName, FallbackValue='請選擇工單'}" Background="Transparent" BorderThickness="0,0,0,1" BorderBrush="Black" FontSize="16" Padding="6,2" HorizontalAlignment="Stretch" Command="{Binding SelectWorkOrderCommand}" />

                <!-- 標題：選擇電極 -->
                <TextBlock Grid.Row="2" Text="選擇電極" FontSize="16" FontWeight="Bold" Margin="0,24,0,8" Foreground="#444" />

                <!-- 下拉按鈕：電極 -->
                <Button Grid.Row="3" Content="{Binding SelectedElectrodeNameDisplay, FallbackValue='請選擇電極'}" Background="Transparent" BorderThickness="0,0,0,1" BorderBrush="Black" FontSize="16" Padding="6,2" HorizontalAlignment="Stretch" Command="{Binding SelectElectrodeCommand}" />
            </Grid>

            <!-- ✅ Select 按鈕 -->
            <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Button Content="確認" Width="160" Height="42" Style="{StaticResource PrimaryButtonStyle}" Background="#2779A7" Command="{Binding ConfirmSelectCommand}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
