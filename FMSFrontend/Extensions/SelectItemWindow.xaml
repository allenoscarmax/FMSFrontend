<Window x:Class="FMSFrontend.Extensions.SelectItemWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:i="http://schemas.microsoft.com/xaml/behaviors" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
        xmlns:conv="clr-namespace:FMSFrontend.Converters"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="選擇" Height="576" Width="560" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Window.Resources>
        <conv:KeyDownEnterOnlyConverter x:Key="KeyDownEnterOnlyConverter" />
        <!-- 簡化的灰色按鈕樣式：Enabled 灰色，Disabled 淡灰 -->
        <Style x:Key="GraySelectableButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="Gray" />
            <Setter Property="Foreground" Value="White" />
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#B3B3B3" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <!-- 外框 + 陰影 -->
    <Border CornerRadius="12" Background="White" BorderBrush="#DDD" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.25" />
        </Border.Effect>

        <!-- 用 Grid 明確切 4 列 -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48" />
                <!-- Title -->
                <RowDefinition Height="64" />
                <!-- Search -->
                <RowDefinition Height="*" />
                <!-- List -->
                <RowDefinition Height="68" />
                <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- 🟦 Title Bar（顏色依類型改：深灰/藍/橘） -->
            <Border Grid.Row="0" CornerRadius="12,12,0,0" Background="{Binding TitleBrush}">
                <TextBlock Text="{Binding Title}"  Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Margin="16,0" />
            </Border>

            <!-- 🔍 搜尋列（左側圖示 + 底線） -->
            <Grid Grid.Row="1" Margin="24,14,24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <materialDesign:PackIcon Kind="Search" Width="35" Height="35" Foreground="Gray" Margin="4,4,8,0" />

                <Border Grid.Column="1" BorderThickness="0,0,0,1" BorderBrush="#222" VerticalAlignment="Center">
                    <TextBox BorderThickness="0" Padding="4,4,4,6" FontSize="16" Foreground="#888" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="KeyDown">
                                <i:InvokeCommandAction Command="{Binding SearchCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Self}}" EventArgsConverter="{StaticResource KeyDownEnterOnlyConverter}" /> 
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </TextBox>
                </Border>
            </Grid>

            <!-- 📋 清單區（灰底容器 + 白卡片項目） -->
            <Border Grid.Row="2" Margin="24,12,24,0" Background="#F4F4F4" CornerRadius="6" Padding="12">
                <ListView ItemsSource="{Binding ItemsView}" SelectedItem="{Binding SelectedItem}" BorderThickness="0" Background="Transparent">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0,0,0,12" />
                            <Setter Property="Background" Value="White" />
                            <Setter Property="BorderBrush" Value="#E5E5E5" />
                            <Setter Property="BorderThickness" Value="1" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Border x:Name="Card" CornerRadius="6" Padding="0">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Card" Property="BorderBrush" Value="#4078B3" />
                                                <Setter TargetName="Card" Property="BorderThickness" Value="2" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Height="45" Margin="0">
                                <TextBlock FontSize="22" Padding="15,0" VerticalAlignment="Center" Text="{Binding MaterialName}" />
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <Ellipse Width="10" Height="10" Fill="{Binding StatusBrush}" Margin="0,0,8,0" />
                                    <TextBlock FontSize="20" Padding="0" Text="{Binding MaterialState}" VerticalAlignment="Center" />
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>

            <!-- Footer -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button Content="確認" Width="108" Height="36" Margin="16,0" Command="{Binding ConfirmCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource PrimaryButtonStyle}" Background="Gray" />
                <Button Content="取消" Width="108" Height="36" Margin="16,0" Command="{Binding CancelCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource PrimaryButtonStyle}" Background="Gray" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
